stages:
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: seict/etl
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./kubernetes.yml
